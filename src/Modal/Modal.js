import React from 'react';
import { View } from 'react-native';
import { Typography, Icon, XIcon } from '../@ornikar/kitt';

// close button : <Typography.Icon icon={<XIcon />} />

export function Modal({ visible }) {
  return (
    <View>
      <Typography.Text>TODO.</Typography.Text>
    </View>
  );
}
